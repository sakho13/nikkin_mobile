import { StyleSheet } from "react-native"

export const cBorder = StyleSheet.create({
  main: {
    borderWidth: 4,
    borderRadius: 10,
    borderStyle: "solid",
    borderColor: "var(--border-light)",
  },
})
